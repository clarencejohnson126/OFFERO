import { notImplemented } from '@/app/api/v1/_lib/responses';

// Custom Domain verbinden — Entitlement 'custom_domain' (Pro/Abo). Auslieferung M8.
export function POST() {
  return notImplemented('Custom Domain kommt in M8 (Auslieferungs-Slice, Pro/Abo).');
}
