import { notImplemented } from '@/app/api/v1/_lib/responses';

// Text-Generierung (−1 Credit), synchron + Job-Handle. Kern-Slice M4.
export function POST() {
  return notImplemented('Generierung kommt in M4 (Text-Generierungs-Slice).');
}
