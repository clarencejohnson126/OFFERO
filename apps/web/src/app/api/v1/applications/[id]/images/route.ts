import { notImplemented } from '@/app/api/v1/_lib/responses';

// KI-Bilder (Gemini) — Entitlement 'images' (Plus+). Medien-Slice M7.
export function POST() {
  return notImplemented('KI-Bilder kommen in M7 (Medien-Slice, Plus+).');
}
