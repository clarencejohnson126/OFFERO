import { notImplemented } from '@/app/api/v1/_lib/responses';

// Asset-Liste/Status (Bilder/Video/PDF). M7.
export function GET() {
  return notImplemented('Medien-Liste kommt in M7 (Medien-Slice).');
}
