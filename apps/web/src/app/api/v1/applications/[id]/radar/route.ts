import { notImplemented } from '@/app/api/v1/_lib/responses';

// Recruiter-Radar-Signale (Anzeige Pro/Abo, geloggt für alle). M8.
export function GET() {
  return notImplemented('Recruiter-Radar kommt in M8 (Auslieferungs-Slice).');
}
