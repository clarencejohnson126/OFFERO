import { notImplemented } from '@/app/api/v1/_lib/responses';

// Feinschliff (gratis, unbegrenzt) → in-place Update + edit_log (ADR 0005). M5.
export function POST() {
  return notImplemented('Feinschliff kommt in M5 (Wallet/Refine-Slice).');
}
