import { notImplemented } from '@/app/api/v1/_lib/responses';

// 60s-Video (FFmpeg-Lite, async) — Entitlement 'video' (Pro/Abo). Medien-Slice M7.
export function POST() {
  return notImplemented('Video kommt in M7 (Medien-Slice, Pro/Abo).');
}
