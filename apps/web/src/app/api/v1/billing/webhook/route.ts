import { notImplemented } from '@/app/api/v1/_lib/responses';

// Stripe-Webhook (Service-Role, Signatur-Verifikation in M6). Schreibt Credits/Entitlements.
export function POST() {
  return notImplemented('Stripe-Webhook kommt in M6 (Billing-Slice).');
}
