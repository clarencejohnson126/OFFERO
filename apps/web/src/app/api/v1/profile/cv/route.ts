import { notImplemented } from '@/app/api/v1/_lib/responses';

// CV-Upload + parse (INGEST) kommen in M3.
export function POST() {
  return notImplemented('CV-Upload/Parse kommt in M3 (Auth+Profil-Slice).');
}
